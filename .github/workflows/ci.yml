name: CI

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev

permissions:
  contents: read
  actions: read

jobs:
  test:
    name: Build & Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ["1.22", "1.25"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ matrix.go-version }}

      - name: Verify Go installation (debug)
        run: |
          echo "=== go version ==="
          go version || true
          echo "=== which go ==="
          which go || true
          echo "=== go env (selected vars) ==="
          go env GOROOT GOPATH GOMOD GOMODCACHE GOFLAGS || true

      - name: Configure Go environment
        run: |
          # Prefer persistently setting GOPROXY using `go env -w` (supported on modern Go).
          # If not supported, fall back to exporting GOPROXY for the job.
          if go env -w GOPROXY=https://proxy.golang.org,direct 2>/dev/null; then
            echo "Configured GOPROXY via 'go env -w'"
          else
            echo "go env -w not supported; exporting GOPROXY for this job"
            echo "GOPROXY=https://proxy.golang.org,direct" >> $GITHUB_ENV
          fi

          # Persist GOMODCACHE for later steps (cache key & path).
          echo "GOMODCACHE=$(go env GOMODCACHE)" >> $GITHUB_ENV
          echo "GOMODCACHE=$(go env GOMODCACHE)"

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ${{ env.GOMODCACHE }}
          key: ${{ runner.os }}-go-${{ matrix.go-version }}-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-${{ matrix.go-version }}-
            ${{ runner.os }}-go-

      - name: Download modules
        run: go mod download

      - name: Run gofmt (check only)
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "gofmt found unformatted files:"
            echo "$unformatted"
            echo "Run 'gofmt -w .' to fix."
            exit 1
          fi

      - name: Run go vet
        run: go vet ./...

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v4
        with:
          version: v1.64.8
          args: --timeout=3m

      - name: Run tests (race detector) and collect coverage
        run: |
          mkdir -p test-results
          go test -v -race ./... -coverprofile=coverage.out || true
          if grep -q "FAIL" coverage.out 2>/dev/null; then
            echo "One or more tests failed (see coverage.out)"
            exit 1
          fi

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.out

      - name: Archive test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results
